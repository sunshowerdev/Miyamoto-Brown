{
	name: { type: "string" },
	animation: { type: "string" },

	mana_cost: { type: "int" },

	//run every frame, returns true if the player has triggered the ability
	should_start: "def(obj miyamoto_playable player) ->bool
	false
	",
}
